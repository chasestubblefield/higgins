%table.table.table-bordered
  %thead
    %tr
      %th Hostname
      %th Pid
      %th State
  %tbody
    - workers.each do |worker|
      %tr
        %td= worker.hostname
        %td= worker.pid
        %td= worker.state
